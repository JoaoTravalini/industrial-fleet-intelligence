# Industrial Fleet Intelligence Web Dashboard

React, TypeScript, and Vite frontend for the local read-only operational dashboard. The app consumes FastAPI `/api/v1` endpoints through `VITE_API_BASE_URL` and performs no browser-side model inference, anomaly scoring, drift calculation, or direct database access.

See `../../docs/frontend/react_dashboard.md` for architecture and development notes.
